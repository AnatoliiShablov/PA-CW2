#ifndef UTILS_HPP
#define UTILS_HPP

#include <array>
#include <cstdint>
#include <vector>

namespace Utils {

std::vector<std::vector<uint32_t>> getCubeGraph(uint32_t size);

}  // namespace Utils

#endif  // UTILS_HPP
